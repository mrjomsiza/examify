Before working in Firebase Studio

git status
git pull --rebase origin main

After editing in Firebase Studio and Before asking Codex

git add .
git commit -m "Functions deployed at this stage."
git push origin main
